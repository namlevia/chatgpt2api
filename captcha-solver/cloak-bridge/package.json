{
  "name": "cloak-bridge",
  "version": "1.0.0",
  "description": "HTTP wrapper around cloakbrowser so the Python captcha-solver can drive CloakBrowser without bundling Node into every request path.",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "cloakbrowser": "^0.3.28",
    "playwright-core": "^1.60.0"
  },
  "engines": {
    "node": ">=18"
  }
}
